Is this a regression, classification, or clustering problem?
It's a classification problem, becuase the output is classes not numbers

Is there a target variable?
yes, the target variables are "spam" or "ham"

What is the model expected to learn or predict?
The model is expected to predict a categorical label for every incoming text message, it predicts if a message belongs to the 
category of "spam" or "ham"